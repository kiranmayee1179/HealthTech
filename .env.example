# Copy this file to .env and add your own Gemini API key.
# Never commit the real .env file or expose this key in frontend code.
GEMINI_API_KEY=your_gemini_api_key_here
# Optional. If omitted, the backend discovers a compatible generateContent model.
GEMINI_MODEL=gemini-3.6-flash
PORT=3001
